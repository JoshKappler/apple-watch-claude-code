{
  "name": "@pinch/simulator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-based Apple Watch Ultra simulator — the reference Pinch client that speaks the exact wire protocol over WebSocket.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pinch/protocol": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.0"
  }
}
